[pytest]
addopts = --strict-config --strict-markers --cov=auto_subtitle --cov=modules --cov-branch --cov-report=xml --cov-report=term
testpaths = tests
python_files = test_*.py
markers =
    e2e: tests requiring real media or machine-learning dependencies
filterwarnings =
    error
