Clears the last two SonarQube Cloud findings on `main` and fixes a PowerShell
lint check that never ran. No changes to the subtitle pipeline, CLI flags,
configuration, or deliverable filenames.

## 🧹 Cognitive complexity

Two functions in `.github/scripts/Invoke-PowerShellLint.ps1` were over
SonarQube Cloud's cognitive-complexity limit of 15. They are split into small
helpers. A before/after comparison over every PowerShell function in the
repository produced identical complexity counts.

## 🐛 Nesting-depth check now runs

The lint's `-MaxNestingDepth 4` check always measured 0: it updated its maximum
from inside a script block, which PowerShell runs in a child scope. Depth is
now measured correctly. Every existing script is within the limit, and a
function nested deeper than four levels is now rejected.

## Breaking changes

- None.
